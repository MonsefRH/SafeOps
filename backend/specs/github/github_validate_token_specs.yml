tags:
  - GitHub Repos
summary: Validate GitHub token and repos
description: Validates provided GitHub token and selected repos. Requires JWT.
consumes:
  - application/json
produces:
  - application/json
parameters:
  - in: body
    name: body
    required: true
    schema:
      $ref: '#/definitions/GithubValidateTokenRequest'
security:
  - Bearer: []
responses:
  200:
    description: Token validated successfully
    schema:
      $ref: '#/definitions/GithubValidateTokenResponse'
  400:
    description: Invalid input or token error
    schema:
      $ref: '#/definitions/GithubErrorResponse'
  500:
    description: Server error
    schema:
      $ref: '#/definitions/GithubErrorResponse'
