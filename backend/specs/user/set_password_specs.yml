tags:
  - User Authentication
summary: Set user password
description: Sets or updates the user's password (requires valid access token).
consumes:
  - application/json
produces:
  - application/json
parameters:
  - in: body
    name: body
    required: true
    schema:
      $ref: '#/definitions/SetPasswordRequest'
security:
  - Bearer: []
responses:
  200:
    description: Password set successfully
    schema:
      $ref: '#/definitions/SetPasswordResponse'
  400:
    description: Invalid input or error
    schema:
      $ref: '#/definitions/ErrorResponse'
  500:
    description: Server error
    schema:
      $ref: '#/definitions/ErrorResponse'
